# 清华 AI Agent 方案（Tsinghua Agent）

> 目标：一个可以**长程执行**的清华事务个人 AI Agent——能查、能办、能主动盯事，覆盖在清华生活学习相关的几乎全部事务。
>
> 日期：2026-09-18。基于对 reference/thu-info-app（v3.17.0）的完整代码调研。

---

## 0. 调研结论（为什么这个方案可行）

`thu-info-app` 的价值在 `packages/thu-info-lib`（约 5700 行 TS）：

1. **纯 Node.js 可用**。协议层只用 `cross-fetch / cheerio / iconv-lite / sm-crypto / jsencrypt / crypto-js / dayjs`，React Native 原生模块（RTNNetworkUtils）是 try/catch 可选依赖。**不需要移植任何原生代码。**
2. **单一入口类 `InfoHelper`，约 90 个公开方法**，覆盖 30+ 类清华事务（详见附录 A）。
3. **鉴权已被完整逆向**：WebVPN OAuth → id.tsinghua.edu.cn SM2 国密加密登录（可选 2FA）→ 对每个子系统做 roam()（用各系统硬编码的 yyfwid 建立"目标系统会话"）。校内校外统一走 WebVPN URL 重写。
4. **自带 mock 模式**（账号密码都是 `8888`），无凭据也能开发测试，`src/mocks/` 里有全套真实形状的假数据。
5. **app 内已有 AI 雏形**：内置 DeepSeek 聊天页，走清华自研大模型平台 MADMODEL（OpenAI 兼容接口，`helper.getMadModelToken()` 经 CAS 鉴权换取 token），已支持 tool_calls——但只接了 1 个工具（写日程）。**本方案就是把这个雏形做完整。**
6. **app 没有**的东西（即我们要新建的）：后台定时任务、通知推送、长程任务编排、工具级权限控制、记忆。app 所有数据都是打开页面时现拉的。

**协议层的坑（架构必须迁就）：**
- `src/utils/network.ts` 的 cookie jar 是**进程级全局单例**（按 name 平铺，不分域），且 WebVPN 依赖手动逐跳重放 cookie + 最多 10 跳重定向。→ **一个进程只跑一个用户会话**（个人 agent 无影响），不要用严格 cookie 库替换它。
- **没有 refresh token**。会话过期 = `verifyAndReLogin`（探针 + 全量重登），lib 已内置自动重试；密码需常驻内存。
- **三种图形验证码**：选课（CR）、体育订场、usereg 网络自助。→ 需要"人机协同"通道（把验证码图推给用户，用户回码）。
- **二次认证（2FA）**：微信 / 短信 / TOTP 三通道。→ **TOTP 是唯一可全自动的**；首次登录时用稳定 fingerprint 注册受信设备，之后免 2FA。lib 提供 `twoFactor*Hook` 钩子。
- 上游页面是 GBK/GB2312 老式 HTML + 正则/cheerio 解析，**天然易碎**；lib 的 `roamingWrapper` 已带"重 roam → 校验重登 → 重试"逻辑，工具层要做超时与降级。

**许可证**：BSL 1.1（非商业）。自用、给同学自用 OK，不能商业化/上架分发。

**最大能力缺口**：lib 不含**网络学堂（learn.tsinghua.edu.cn）**——作业/课件/DDL。这是学生事务里最有"长程监控"价值的一块，由 robertying 的 **thu-learn-lib**（learnX 同源库）补齐，作为第二协议层接入。

---

## 1. 总体架构

```
┌─────────────────────────────────────────────────┐
│ 交互层（终态：自托管 Web 控制台）                   │
│  聊天对话 · 数据仪表盘 · 确认门/验证码回传           │
│  · 任务管理面板 · 通知推送(Bark/微信)               │
│  开发期（Phase 0–2）用 Claude Code/CLI，零 UI 工作  │
└───────────────┬─────────────────────────────────┘
                │
┌───────────────▼─────────────────────────────────┐
│ Agent 运行时                                      │
│  · Claude Agent SDK（推荐）或任意 LLM 的工具循环    │
│  · 长程会话：多步规划、中途 checkpoint 到 SQLite    │
│  · 系统提示注入：校历/当前周次/课表/待办上下文       │
└───────┬──────────────────────────┬──────────────┘
        │ MCP                      │ 任务下发/事件回报
┌───────▼──────────────┐  ┌────────▼──────────────┐
│ 工具层                │  │ 常驻守护进程 supervisor │
│ thu-mcp-server       │  │  · node-cron 定时轮询   │
│ ~90 个工具，三级分类： │  │  · 监控器（GPA/电费/座位 │
│  read / write /       │  │    /DDL/新闻/选课余量）  │
│  write+pay(高风险)    │  │  · 通知器（Bark/ntfy/   │
│  write 需确认门控      │  │    邮件/微信）           │
└───────┬──────────────┘  │  · 会话保活 + 重登       │
        │                 └────────┬──────────────┘
┌───────▼──────────────────────────▼──────────────┐
│ 协议层（单进程单会话）                             │
│  @thu-info/lib（主）+ thu-learn-lib（网络学堂）    │
│  + Playwright 兜底（lib 未覆盖的偶发事务）          │
├─────────────────────────────────────────────────┤
│ 状态层  SQLite：动作日志 / 任务表 / 监控规则 / 记忆  │
│ 凭据   macOS Keychain（或 chmod 600 .env）        │
└─────────────────────────────────────────────────┘
```

**两个进程，职责分离：**
- **agent 进程**（按需启动）：理解自然语言 → 规划 → 调工具 → 多步执行长任务。
- **supervisor 进程**（常驻）：定时轮询、触发通知、执行 agent 下发的"长期任务"（如"出成绩就告诉我"、"电费低于 20 度提醒"、"明早 8 点帮我抢 A 课，失败自动试 B"）。它的逻辑是确定性代码 + 小模型摘要，**不需要大模型常驻**，省 token 且可靠。

工具层做成 **MCP server** 是关键决策：一份工具代码，同时服务 Claude Code / Claude Desktop / 自研 runtime，v1 不用写任何 UI。

---

## 2. 关键设计决策

| # | 决策点 | 建议 | 理由 |
|---|--------|------|------|
| 1 | Agent 运行时 | **Claude Agent SDK**，LLM 用 Claude；保留 OpenAI 兼容接口作为备选（可切清华 MADMODEL 免费 DeepSeek） | SDK 自带长程会话、权限门、hooks、MCP；MADMODEL 免费但 tool-use 能力弱，适合做摘要类副驾 |
| 2 | 工具暴露方式 | **MCP server**，90 个 lib 方法**不要**一比一全暴露——聚合成 ~40 个语义化工具（如 `query_transcript`、`book_library_seat`、`search_courses`），避免工具表过载 | 模型选工具的准确率随工具数下降；聚合点也是确认门控点 |
| 3 | 写操作安全 | 三级分类：`read`（直接放行）/ `write`（agent 须先展示参数，用户确认）/ `write+pay`（挂失、改密、缴费、选课、退课——必须人工确认，缴费类自然有支付宝扫码这道闸） | "帮我把事务办了"和"别帮我把卡挂失了"必须同时成立 |
| 4 | 2FA / 验证码 | 首次配置走 TOTP + 受信设备注册（一次人工，长期免打扰）；三类验证码走通知通道把图推给用户、轮询收码 | lib 的 hook 机制就是为此设计的 |
| 5 | 长程执行 | 三种模式：a) **会话内长任务**（checkpoint 续跑）；b) **cron 监控**（确定性检查 + 通知）；c) **长期任务表**（agent 写入 SQLite 的"直到满足条件为止"任务，supervisor 每轮检查、条件满足时唤起 agent 汇报） | "长程"≠"LLM 常驻轮询"，成本和可靠性都要求把"等"交给确定性代码，把"判断"交给模型 |
| 6 | 请求礼仪 | 所有轮询有最小间隔（成绩/新闻 10min，电费 30min，选课余量在选课高峰 30s）；单会话串行请求 | lib 是逆向协议，别把账号跑出风控特征 |

---

## 3. 分阶段路线图

### Phase 0 — 地基（0.5 周）
- monorepo 脚手架（pnpm workspace）：`packages/mcp-server`、`packages/supervisor`、`apps/cli`。
- `@thu-info/lib` 以 `file:`/子模块引入 reference 里的包。
- 登录与保活：稳定 fingerprint、TOTP 2FA hook、受信设备注册、`loginErrorHook`；`getCsrfToken()` + `getUserInfo()` 作为登录验证。
- 验收：mock 模式全工具冒烟 + 真实账号登录成功 + 睡眠 8h 后自动重登成功。

### Phase 1 — 只读工具 + CLI 对话（1 周）★最快可用
- MCP server：~25 个 read 工具（成绩、课表+当前周、校历、卡余额/流水、电费、图书/座位、研读间、新闻检索、教室占用、培养方案完成度、THOS 待办、网费/在线设备、发票、体育记录……）。
- 会话启动注入上下文：校历、今天第几周、课表、卡余额。
- Claude Code 挂上 MCP 即可用："我下周有什么课""卡里还有多少钱""帮我找周三晚上空着的教室"。
- 验收：三个真实场景问答零失败。

### Phase 2 — 写操作 + 确认门（1–2 周）
- write 工具：选课（选/退/志愿/PF）、图书馆座位与研读间预约/取消、体育订场、教学评估、新闻订阅、日程增删、发邮件（`naiveSendMail`）、网络设备认证/下线。
- 确认门：CLI 内 diff 式确认；CLI 不在线时通知渠道确认。
- 支付类（电费、卡充值、体育订场付费）：生成支付宝 pay-code 二维码推给用户——天然人工闸。
- 验收：用 mock 模式跑通选课全流程脚本；真实环境只实测低风险项（订阅新闻、订座再取消）。

### Phase 3 — 长程执行核心（2 周）★本项目灵魂
- supervisor 常驻进程：node-cron + SQLite 任务表 + 通知器（Bark/ntfy 优先，清华邮箱 `naiveSendMail` 兜底）。
- 内置监控器（确定性代码）：GPA/单科成绩（THOS `getScoreByCourseId`）发布监控、卡余额阈值、电费阈值、图书馆预约违约风险提醒、新闻关键词、选课阶段时间点（`getCrTimetable`）、**网络学堂 DDL**（thu-learn-lib）。
- 长期任务表 + agent 注册接口："出分了叫我""选课补选阶段开始提醒我抢 X 课，余量>0 就通知我，我确认后自动选"。
- 事件触发 agent：监控器发现事件 → 起 agent 会话 → 汇总 + 建议 → 推送。
- 验收：跨 7 天稳定运行，至少一次"事件 → 通知 → 我下指令 → agent 办成"闭环。

### Phase 4 — 能力补全（2–3 周，按需取舍）
- **thu-learn-lib 接入**：课程/作业/DDL/课件/通知（读为主，交作业写操作谨慎）。
- THOS 服务办理（lib 只读已通，表单提交用 Playwright 按 service 深链半自动填写 + 人工确认）。
- Playwright 兜底通道：复用会话 cookie，覆盖 lib 没有的长尾事务（如个别缴费、场馆新系统）。
- 交互升级：**自托管 Web 控制台（已选定终态）**。建议 supervisor 进程内嵌 HTTP + WebSocket 服务（一个进程同时是守护进程和网站后端），前端 React（Vite）打包静态文件由同一服务托管，手机/电脑浏览器访问即可，无需独立部署。页面五块：①与 agent 的聊天对话 ②仪表盘（课表/卡余额/电费/网费/待办 DDL）③确认门卡片（写操作参数 + 确认/拒绝按钮）④验证码回传 ⑤长期任务管理（查看/暂停/删除 cron 与监控规则）。
- 微信/Telegram bot 可作为后续附加入口（复用同一工具层），不做。
- 记忆：用户偏好（常订哪个馆、常用收件抬头、选课口味）存 SQLite，会话启动注入。

### Phase 5 — 打磨
- 选课策略工作流（方案 A/B/C 自动降级 + 余量监控 + 确认后秒选，验证码人机协同）。
- 周报（本周课表/消费/借书到期/DDL 汇总，每周日晚推送）。
- 多账号支持需评估（cookie jar 是进程级全局——每用户独立进程即可，暂不做）。

---

## 4. 风险与对策

| 风险 | 对策 |
|------|------|
| 上游改版导致解析失效 | 工具层捕获解析异常 → 明确报"此功能失效"而非静默错数据；mock 测试保底回归；上游仓库活跃（2026-09 仍在更新），跟进 patch |
| 账号风控 | 低频轮询、固定 UA（lib 已固定 Chrome 79）、单会话串行、夜间不跑重任务 |
| 凭据泄漏 | Keychain 存储、日志脱敏（password/cookie/token 全遮蔽）、`secrets.json` 永不入库。**注意清华一码通：门户密码≈校园卡交易密码**，.env 泄漏≈卡内资金可被操作 → `chmod 600 .env` + FileVault + 高危卡操作强制人工确认（确认门不因"已有密码"而放行） |
| 危险写操作误执行 | 三级分类 + 高风险二次确认 + 动作日志可审计（SQLite 留痕） |
| 验证码无法自动化 | 定位为"人机协同"而非全自动：推图收码，秒级往返；选课抢跑场景提前把验证码备好（lib 支持先取 captcha URL） |
| BSL 许可证 | 仅自用/校内非商业分享，不公开发布二进制 |
| lib 缺网络学堂等模块 | thu-learn-lib 补位；再缺的用 Playwright 兜底 |

---

## 5. 下一步

1. `pnpm init` 起仓库，引入 thu-info-lib，跑通 mock 登录 → Phase 0。
2. 先写 **5 个最有体感的 read 工具**（课表+周次、成绩、卡余额流水、电费、空闲教室）挂进 Claude Code 验证"对话即办事"的体验。
3. 体感对了再铺全量工具和 supervisor。

## 附录 A：@thu-info/lib 能力地图（read/write）

- **门户/学业**：成绩单与 GPA、教学评估（读/交）、体检成绩、空闲教室、电子发票、校历、培养方案完成度、THOS 单科成绩/待办流程、倒计时、门户邮件（发）
- **选课 CR**：课表时间轴、开课计划、余量检索、选/退课、志愿调整、PF 设置、排队信息、优先级统计（全部含写）
- **网络学堂**：lib 无 → thu-learn-lib 补位
- **图书馆**：馆区/楼层/区域/座位（含插座状态）、订座/取消、研读间预约/取消、借书记录、教参平台检索
- **体育**：8 场馆资源与价格、订场、付款（支付宝码）、退订、手机号维护
- **校园卡**：余额/状态、流水（消费/充值/补助）、密码修改、日限额、**挂失/解挂**、银行圈存、微信/支付宝充值
- **宿舍**：电费余额/充值（支付宝码）、充值记录、卫生评分、服务密码重置
- **校园网**：余额、账户信息、在线设备、认证/下线、usereg 登录（验证码）
- **信息门户**：新闻多频道列表/检索/订阅/收藏/正文（含 PDF）
- **课表**：合并一二课表 + 选课课表；个人日程增删
- **其他**：GitLab 全套（仓库/分支/文件）、工资/银行卡发放记录（含研究生）、MADMODEL 大模型 token
